# Turtle Graphics: Modularizing Code with Functions

# Reading

import turtle

turtle.fillcolor('blue')
turtle.begin_fill()
for count in range (4):
    turtle.forward(100)
    turtle.left(90)
turtle.end_fill()

# Below is extra so program doesn't close.
turtle.done()

# Check other .py files in the folder for more info.